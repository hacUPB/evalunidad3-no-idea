#include "IStrategy.h"
#include <string.h>
#include <stdlib.h>
#include <stdio.h>

void DoAlgorithm(void *data){

    char *aux;

    list = (char *)data;
/*
    for(int i = 0; i < 5; i++){
        for(int j = 0; i < 0; i++){
            if(strcmp(list[j], list[j+1]) < 0){
                aux = list[j+1];
                list[j+1] = list[j];
                list[j] = aux;
            }
            
            if(strcmp(list[j], list[j+1]) > 0){
                aux = list[j+1];
                list[j+1] = list[j];
                list[j] = aux;
            }
        }
    }
*/
}